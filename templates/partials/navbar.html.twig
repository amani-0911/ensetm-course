<nav class="navbar navbar-expand-lg navbar-dark " style="background: #2051a9;color: #fff;">
    <a class="navbar-brand" href="{{ path('accueil') }}">ensetm-course</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav mr-auto">
            <li class="nav-item ">
                <a class="nav-link {% if current_menu is defined and current_menu == 'visité' %}active{% endif %}" href="{{ path('articles.index') }}">Voir Les Cours</a>
            </li>
            {% if is_granted('ROLE_PROF') %}
            <li class="nav-item ">
                <a class="nav-link {% if current_menu1 is defined and current_menu1 == 'visité' %}active{% endif %}" href="{{ path('articles_index') }}">Ajouter</a>
            </li>
            {% endif %}
            {% if is_granted('ROLE_ADMIN') %}
                <li class="nav-item ">
                    <a class="nav-link " href="\admin">Admin</a>
                </li>
            {% endif %}
        </ul>

            <ul class="navbar-nav">
                <li class="nav-item">
                    <a class="nav-link"  href="{{ path('app_logout') }}"> Se déconnecter</a>
                </li>
            </ul>
    </div>
</nav>
